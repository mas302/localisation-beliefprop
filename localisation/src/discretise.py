import numpy as np
from scipy.stats import multivariate_normal
from scipy.integrate import dblquad

def discretise(limits, cardinalities):

    probabilities = np.zeros(limits)

if __name__ == '__main__':
    print(np.zeros([1,2,3]))
